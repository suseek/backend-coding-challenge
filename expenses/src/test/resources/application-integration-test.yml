spring:
  profiles: integration-test
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=${flyway.schemas}
    username: engagetech
    password: engagetech
    hikari:
      idle-timeout: 10000
  jpa:
    show-sql: true
    properties:
      hibernate:
        default_schema: ${flyway.schemas}
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  security:
    enabled: false

endpoints:
  enabled: false
  health:
    enabled: true
  metrics:
    enabled: true

flyway:
  schemas: expensesTest